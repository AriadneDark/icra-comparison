# SVG2 — annotation pipeline, TRASER training and TRASER inference, one environment.
# See the README for the four install commands; torch, the captioner and flash-attn are
# installed separately because each needs a flag this file cannot carry.

transformers==4.54.1
accelerate>=1.4
huggingface_hub>=0.34          # provides the `hf` CLI used in docs/DATA.md
deepspeed>=0.16
sentencepiece
protobuf
einops
timm

# Annotation pipeline, stages 1-2: segmentation and tracking.
# (The project installs under the name SAM-2, so it is listed as a bare URL.)
hydra-core>=1.3
iopath
git+https://github.com/facebookresearch/sam2.git

# Annotation pipeline, stages 5-6: OpenAI API.
openai>=1.40

# Masks, video decoding, arrays, config.
pyarrow                        # traser/data/prepare_svg2.py reads the released parquets
# torchcodec is an optional fallback decoder, used only when decord fails on a file; it needs
# FFmpeg shared libraries (e.g. conda install -c conda-forge 'ffmpeg<8') to be importable.
pycocotools>=2.0.7
opencv-python-headless>=4.9
decord>=0.6
torchcodec>=0.7
numpy
pillow>=10
pydantic>=2.5
pyyaml>=6
tqdm>=4.66
wandb
